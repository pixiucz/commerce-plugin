# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    name:
        label: pixiu.commerce::lang.fields.name

    rate:
        label: pixiu.commerce::lang.fields.rate
